m_shield_demo — Shield AI Demo 類中線任務 (MOOS-IvP)

任務摘要:
  - 兩艘子船 (abe, ben) 於任務區域內協同搜索
  - 一艘模擬目標船 (target) 在任務區內巡邏，供子船偵測與跟隨
  - 母船 (mothership) 作為艦隊協調中心 (分區 + 燕型夾角指派)
  - Shoreside 提供操作員 GUI 與通訊轉發

MOOS 社群架構:
  社群           MOOSDB Port   pShare Port
  ----------     -----------   -----------
  shoreside      9000          9200        (operator GUI + vehicle broker)
  abe (USV1)     9001          9201        (sub-vessel 1)
  ben (USV2)     9002          9202        (sub-vessel 2)
  target         9003          9203        (simulated intruder)
  mothership     9005          9205        (coordinator)

通訊流向:
  操作員指令   shoreside --(uFldShoreBroker/pShare)--> abe, ben, target
  船位回報     abe/ben/target --(uFldNodeBroker)--> shoreside
               abe/ben/target --(pShare 直送)------> mothership
  任務區域     shoreside (MISSION_POLY, DEPLOY_ALL) --> mothership
  艦隊任務     mothership (WPT_UPDATE_*, TRAIL_UPDATE_*) --直送--> abe, ben

  注意: 母船的指令「不能」經由 shoreside 中繼。pShare 若對某個變數同時
        存在 input 與 output 路由，就拒絕把收到的那份注入本地 MOOSDB，
        而 uFldShoreBroker 的 qbridge 正好會建立這種路由。

啟動方式:
  ./launch.sh [time_warp]
  例如: ./launch.sh 10    (10倍加速模擬)
       ./launch.sh 1     (即時)

劃定搜索區域 (兩種方式):
  A. 滑鼠標點 (建議): 在 pMarineViewer 的滑鼠 context 選單選 "region"，
     然後在地圖上左鍵點各個角 (3 點以上)。pRegionDivider 取這些點的
     凸包當作區域，所以點的順序無所謂，多點會持續修正形狀。
  B. 按 REGION:DEFAULT 按鈕，套用內建的預設區域。
  按 REGION:CLEAR 可清掉重畫。

  區域確定後會依子船數量自動由西向東等分 (目前 2 艘 = 2 條)，
  未來加船只要擴充 vnames 清單即可，不需改程式。

掃描方式 (SEARCH_PATTERN，可在任務進行中隨時切換，切換即刻重新規劃):

  除了 perimeter (依定義只走外框) 之外，每一種都會把自己的分帶完整
  走過一遍。以感測半徑 15m、預設區域實測的覆蓋率與路徑長度:

    pattern     航點   長度(m)   覆蓋率
    lawnmower      6      831    100.0%
    skip           6      858    100.0%
    spiral         4      551     99.7%
    perimeter      4      551     99.7%
    sector        62     1664     99.7%
    barrier       22      699     99.7%
    figure8      129     1926    100.0%

  (剩下的 0.3% 是取樣格網落在區域角落的單一格。)

  車道掃描型:
    lawnmower  相鄰車道往返。總距離最短，但每個 180 度迴轉只有一個
               車道寬，是所有方式裡轉最硬的。
    skip       去程走單數車道、回程補雙數車道。迴轉半徑變成兩個車道
               寬，這才是真正讓迴轉變鬆的作法。
    spiral     由外往內的矩形螺旋。
    perimeter  只走分帶的外框一圈。唯一不做面覆蓋的方式 — 覆蓋格線
               中間留白是正確結果，不是規劃出錯。

  搜索 / 巡航型:
    sector     扇形搜尋。以分帶「中心」為基準點，360 度放射狀掃出去，
               每條腿到分帶邊界為止。腿數由最長的那條腿決定，所以相鄰
               兩腿即使在最遠處也不超過一個車道寬，其餘位置只會更密 —
               這就是它既是扇形、又能把分帶掃完的原因。
               基準點附近的水域會被重複掃過很多次，目標最後已知位置在
               某一點附近時用這個，車道型掃描做不到這種密度分布。代價
               是距離: 約為 lawnmower 的兩倍。

               註: 基準點原本放在分帶「近端」，但長條分帶從那裡張開的
               扇形只有幾度寬，航線全擠在中線附近，近半段的外側角落
               根本沒被碰到 — 看起來像扇形，卻沒在搜索。

    barrier    屏障攔截巡航。警戒線「橫越」車道軸 (也就是橫越區域長軸、
               橫越任何穿越任務區的船最可能的航向)，跑完一條就沿分帶
               推進一個車道寬再跑一條，等於把這道屏障沿著區域走一遍。
               這和換個名字的 lawnmower 不同: lawnmower 的車道「順著」
               長軸，少而極長; 這裡的腿橫越長軸，多而短，子船的時間
               花在穿越通行方向上而不是與它平行。

               註: 原本只有分帶中線一條線來回跑，除了那條線以外整個
               分帶從沒被走過。另外警戒線的擺放範圍不能沿用修剪過的
               u 範圍 — 那個修剪是因為「沿車道前進時感測器已看到前方」，
               對橫向線並不成立，沿用會在分帶兩端各留下一條空白。

    figure8    八字形巡航。沿著分帶串起一連串 Gerono 雙紐線 lobe，每個
               lobe 橫跨整個分帶寬度。去程每個 lobe 走 t∈[π,2π] (從左端
               進、右端出，正好是下一個 lobe 的左端)，回程走 [2π,3π] 補
               另一半，所以整條路徑是單一封閉迴圈，沒有任何重複航段。
               全程沒有 180 度迴轉，轉彎都是淺而固定半徑的，速度幾乎不掉。

               lobe 半長取 0.6 個車道寬，這是覆蓋的代價: 感測器最難搆到
               的點是交叉點正上方的分帶邊緣，航跡最接近它的距離是
               0.707*A。0.6 車道寬時約為感測半徑的 0.76 倍，看得到; lobe
               再拉長，每個交叉點的上下就會開洞。

               註: 原本是一條橫跨整個分帶的雙紐線。雙紐線是一條「線」
               不是一個「面」，250m 的分帶幾乎整片沒走到。

  所有方式都會 repeat (預設 999 圈)。以前子船跑到最後一個航點就停在
  原地不動 — 對巡航型完全說不通，對覆蓋型也等於後半場整支船隊閒置。
  而且自動接戰要能運作，子船被解除攔截後必須回到搜索並繼續動。

掃描覆蓋顯示:
  子船掃過的格子會在地圖上以 VIEW_GRID 淺色標示，方便看還有哪裡沒掃。
  格子大小與感測半徑在 pRegionDivider 的 cell_size / sensor_radius 設定。

啟動後在 pMarineViewer GUI 按下按鈕:
  1. DEPLOY   — 部署所有船艦，並「直接」進入搜索模式
  2. SURVEY   — 手動切回搜索模式
  3. INTERCEPT — 提早觸發接戰 (子船在目標船的「內側」形成 120 度包夾弧)。
                 不是鎖存: 驅離成功後一樣會自動解除，詳見「自動接戰循環」。
  4. RETURN   — 所有船回港
  5. STATION:true / STATION:false — 定點待命
  9. REGION:CLEAR   — 清掉目前區域，重新標點
  10. REGION:DEFAULT — 套用內建預設區域
  11-13. TGT:UNDO / TGT:CLEAR / TGT:APPLY — 目標船航線編輯

選單列 Action 下拉選單 (取代原本的 PAT:* / SEE:* 按鈕):
  Action > Search Pattern > ...  掃描方式
  Action > Sensor Range  > ...   視距 / 偵測距離

  這兩項原本各佔 4 個與 3 個按鈕，共 7 顆，而按鈕面板上限只有 20 顆。
  改成下拉選單後，一個設定就是一個選單、底下是具名的選項，之後要再加
  掃描方式也不必再挪按鈕。

  下拉選單靠 pMarineViewer 的 action 參數加上新的 menu_label 欄位:
      action = SEARCH_PATTERN = sector # menu_label = Search Pattern/Sector Fan Search
  menu_label 裡的 '/' 就是子選單路徑，點一次會把該筆的所有變數一起送出。
  沒有 menu_label 時仍是舊行為 (選單項目直接標成 "變數=值")。

  每顆按鈕都會送出「完整」的模式狀態 (而不是只送有變動的那幾個)，
  所以任務可以任意順序切換、也可以重複跑第二輪而不會殘留舊旗標。

  DEPLOY 之後 pRegionDivider 會立刻發出分區航線，並在接下來幾秒內
  重送幾次，以確保不會跟 helm 建立 behavior 的時序賽跑。
  (helm 要 DEPLOY 之後才會建立 behavior，太早發的 update 會被丟棄。)

清除 log 檔案:
  ./clean.sh

驅逐機制 (包夾式):
  pTargetCoordinator 不再把子船排在目標船的正後方跟車，而是把它們
  放在「目標船 → 任務區中心」這條方位線上、左右各 60 度的位置
  (spread_deg=120, trail_range=40)，用的是絕對方位而非相對方位。
  也就是說子船永遠站在目標船與它想待的水域之間，形成一道牆。

  目標船自己也跑 pContactMgrV20 + BHV_AvdColregsV22。它的避碰行為
  (pwt 250, 高於巡邏的 100) 被壓迫時會往開放水域讓開，因而被推出
  任務區，而不是被追著跑。

  站位距離 40m 必須同時滿足兩個條件: 要在目標船自己避碰的作用範圍
  內 (pwt_outer_dist 55) 才有持續壓力; 又要在子船「對目標船」那組
  避碰的作用範圍外 (pwt_outer_dist 30)，否則跟車與避碰兩個行為會
  互相拉扯，實際通過距離比任一方要求的都近。min_standoff=30 是硬
  下限，避免日後把 trail_range 改小又掉回去。

  地圖上會用紅色線段 (VIEW_SEGLIST "pincer") 畫出兩支包夾臂，以及
  兩個紅色站位點，讓包夾隊形一目了然。

  速度差: 目標船 0.8 m/s (上限 1.2)，子船 2.0 m/s (上限 3.0)。
  子船有 2.5 倍的速度餘裕，可以從場地任一角落趕上並保持站位。

  脫離航道 (escape_lane_deg=70):
    目標船右舷保留 70 度的淨空扇形，包夾弧若壓到它就整段旋轉讓開。
    理由見下面的「國際避碰規則 (COLREGs) 遵循」一節。


國際避碰規則 (COLREGs 1972) 遵循:

  參考: IMO COLREG 公約本文 (Rule 8/13/14/15/16/17)。

  ── 原本不合規的地方 ──────────────────────────────

  1) 避碰完全沒有「側」的概念
     原本用 BHV_AvoidCollision，其目標函式 AOF_AvoidCollision::evalBox()
     只用投影 CPA 距離評分，沒有任何項與「對方在左舷或右舷」「誰是
     讓路船」有關。於是對遇可能左對左、也可能右對右; 交叉相遇時
     兩艘都閃。牴觸 Rule 14(a) (對遇雙方各自向右轉、左舷對左舷通過)、
     Rule 15 (右舷有他船者為讓路船，且應避免橫越他船船首)、
     Rule 16 (讓路船應及早、大幅度地避讓)、
     Rule 17(a)(i) (直航船應保持其航向及航速)、
     Rule 13 (追越船應讓路 — 子船速度是目標船的 2.5 倍，幾乎永遠
     處於追越狀態)。

  2) 避碰行為根本沒被生成 (最嚴重)
     pContactMgrV20 的 CMAlert::setAlertRangeFar() 會把 alert_range
     夾到 cpa_range:
         m_range_far = dval;
         if(m_range > m_range_far) m_range = m_range_far;
     原設定 alert_range=15, cpa_range=5，實際生成距離是 5 公尺。兩艘
     子船對遇合速 4 m/s，只有 1.2 秒。Rule 8(a) 要求避讓動作
     "made in ample time"，5 公尺談不上。舊 log 實測子船間最近
     距離 5-7 公尺。
     目標船的 alert_range=60, cpa_range=25 同樣被夾成 25 公尺，
     所以舊註解寫的「pwt_outer_dist=55 持續施壓」從未發生。

  3) min_util_cpa_dist=5 不是 Rule 8(d) 所要求的 "safe distance"。

  4) Rule 14 的判定門檻過窄
     BHV_AvdColregsV22 的 m_headon_abs_relbng_thresh 寫死 12 度，比
     Rule 14(b) 以舷燈可見界定的 22.5 度扇形窄。近似對遇同時也過不了
     Rule 15 的交叉判定 (雙方都看到對方在自己右舷，那不是任一方的
     讓路幾何)，於是掉回側向無關的 cpa 模式。實測: 兩船右舷對右舷
     在 14.8 公尺通過，且應讓路的一方向左轉。
     Rule 14(c) 明文「有疑義時應假定對遇存在」，支持採用較寬的扇形。

  5) 包夾幾何讓對方沒有合規解
     對稱夾在兩舷時，目標船對右舷那艘是讓路船，必須向右轉才合規
     (Rule 15/16; Rule 17(c) 禁止向左轉向位於左舷之他船) — 但右邊
     正是另一艘子船。Rule 8(c) 要求避讓「不得造成另一個近距離局面」，
     Rule 8(f) 要求留下「足夠水域供他船安全通過」。

  6) 攔截站位太近
     站位 25m 對上子船「對目標船」避碰的 pwt_outer_dist 20m，站位
     永遠落在避碰作用範圍內，跟車與避碰互相拉扯。實測最近到 9.9m，
     行為回報 standon:inextremis — 已是 Rule 17(b)「僅靠讓路船之
     措施已不能避免碰撞」的狀態。

  以上六項是 COLREGs 相關的。另外還有一個 completed_dist 的設定坑，
  見下面獨立一節。

  ── 修正 ─────────────────────────────────────

  a) 子船與目標船全部改用 BHV_AvdColregsV22。它會把每次遭遇分類成
     headon / giveway / standon / overtaking / cpa，每種模式用不同的
     目標函式 (AOF_R14、AOF_R16、AOF_R17...)。以 AOF_R14 為例，凡是
     會造成右舷對右舷通過、或橫越他船船首的解，效用直接回傳 0 —
     Rule 14 是寫在目標函式裡，不是期待它自己發生。

     子船有兩個實例:
       avdcol_  對其他船，完整距離 (outer 45 / inner 20 / CPA 12-20)
       avdtgt_  對指定入侵者，攔截尺度 (outer 35 / inner 20 / CPA 20-30)
     分開是因為包夾要在 40m 站位上待著; 用 45m 那組的話避碰會一直
     對著 BHV_Trail 正在接近的那艘船作用，兩者互打。

     注意 min_util_cpa_dist 是「效用降到零的距離」，不是硬性約束 —
     helm 是對所有行為的總效用做最佳化，BHV_Trail 拉得夠強還是買得到
     更近的通過距離。實測 CPA 設 15/25 時，abe 從入侵者船尾 9.1m 通過
     (身分與方向都對: 讓路船、且從船尾而非船首過，符合 Rule 15，優先權
     從 28m 外就開始爬升、速度降到 0.4 m/s 符合 Rule 8(e))，但 9m 不是
     Rule 8(d) 的 "safe distance"。放寬到 20/30 後才穩定在 20m 以上。

  b) 三個 contact manager 的 alert 距離全部改成 cpa_range >= alert_range
     (子船 60/75、對入侵者 45/60、目標船 60/75)。

  c) BHV_AvdColregsV22 新增可設定參數 headon_abs_relbng_thresh。
     函式庫預設維持 12 度不動 (相容既有任務)，本任務三個避碰區塊
     都設 22.5 度，對齊 Rule 14(b) 的舷燈扇形。

  d) escape_lane_deg=70: 目標船右舷保留淨空扇形，包夾弧壓到它時
     整段旋轉讓開 (而不是把兩個站位壓到同一邊 — 那會把兩艘子船
     指到同一個方位、同一個距離，等於叫它們互撞)。

  e) trail_range 25 -> 40，並加上 min_standoff=30 硬下限。

  f) min_detect_range=45 (pTargetCoordinator)
     這是 (a) 帶出來的連鎖問題: 子船對入侵者的避碰把最近通過距離
     守在 20m、從 35m 外就開始推開，但偵測距離沿用視距預設的 15m —
     子船會主動閃開到永遠偵測不到目標，整個接戰循環不會啟動。實測
     一整輪掃描 0 次偵測 (同樣時間長度，前一版有 2 次驅逐)。
     偵測距離必須大於子船對入侵者的避碰站開距離，因此加了下限。
     地圖上畫的圈顯示的是實際生效的距離，所以這個提升操作員看得到。

  ── completed_dist 的坑 ──────────────────────────

  completed_dist 必須明顯大於 contact manager 的 alert_range，不能
  接近。行為在 completed_dist*1.1 會自我完成; 設得跟生成距離差不多，
  行為一生成就死，而 contact manager 從沒看到距離跨出去、也就不會
  重發警報，結果是那個接觸永遠停在 "complete"、完全沒有避碰。
  修正過程中我自己踩過: ben 曾整段 17.5 公尺對遇都停在 complete。

  ── 實測驗證 ─────────────────────────────────

  對遇 (兩艘子船指定互為反向航線，重複多次):

    項目             修正前        修正後
    ----------      ---------     -----------------
    通過舷別         右對右        左對左 (7/7 次)
    對遇 CPA         14.8 m        17.8 - 26.6 m
    進入模式距離      27 m          63 - 81 m

  最近距離 (整輪任務，含攔截):

    項目                        修正前     中途      最終
    ------------------------   --------   -------   -------
    子船 - 子船                 5 - 7 m    -         21.5 m
    子船 - 入侵者 (攔截中)       -          9.1 m     21.6 m

  攔截時可在 log 看到完整模式集在運作: giveway:bow、giveway:stern、
  headon、overtaking:port、standon:stern、standon_ot:port /
  standon_ot:starboard (Rule 13 追越)、cpa。

  log 裡還會出現 standon_ot:inextremis。這不是違規，是 Rule 17(a)(ii)
  正常運作: 本船為直航船、對方 (讓路船) 未採取適當措施、且推算 CPA
  會低於可接受下限時，直航船取得自行避讓的權限。實測最近距離 21.6m
  仍高於 20m 下限，代表這個介入是有效的。

  ── 可稽核性 ─────────────────────────────────

  每個接觸都會發布 COLREGS_AVOID_MODE_<CONTACT>，值是當下適用的
  模式，另有 CN_PORT_OF_OS__<CONTACT> (對方是否在本船左舷)、
  AVDCOL_RANGE_<CONTACT>、HEADON_DEBUG_<VNAME> (未進入對遇模式的
  原因)。也就是說「當時適用哪一條規則、實際走哪一舷」可以事後從
  alog 查證，不必用猜的。

自動接戰循環 (pTargetCoordinator，無需操作員介入):

  SEARCHING  子船跑自己的掃描方式。目標船進入任務區、而且進到任一
             子船的偵測距離內 → 自動接戰。
  INTERCEPTING  包夾驅離 (如上)。
  SEARCHING  目標船被推離任務區 (或離開所有子船的偵測範圍)，並且
             持續 release_hold 秒沒有再回來 → 判定「驅逐成功」，
             解除警報、子船切回掃描搜索，等下一次偵測。

  偵測距離就是視距 SENSOR_RADIUS 本身 (detect_range=0 表示沿用它)。
  也就是 Sensor Range 選單選的數字、搜索航線車道間距用的數字、以及
  地圖上畫在子船周圍的圓圈，三者是同一個數字 — 看到的就是會反應的。
  圓圈搜索中是綠色、攔截中轉為橘色。

  「離開偵測範圍」不能單獨當成功條件: 包夾時子船本來就刻意停在
  trail_range (25m) 的站位上，若那個距離就算「離開」，一接戰就會
  立刻宣告成功。所以 release_range 有 2*trail_range 的下限，實際
  結束交戰的判定主要是「離開任務區邊界 15m 以上、持續 8 秒」。
  這個「持續」很重要 — 被壓迫的目標船沿著邊界左右擺盪時會來回進出
  好幾次才真的離開，只看某一瞬間會誤判。

  成功之後有 10 秒的靜默期 (reengage_delay)，讓子船先轉回掃描航線，
  而不是在目標船還在旁邊時立刻重新咬上去。

  操作員按 INTERCEPT 是「提早觸發同一個接戰循環」，不是另一種模式:
  它一樣要通過上面的解除判定。按 SURVEY 則是手動收兵 (不計成功)。

  這裡原本有個 bug: 手動警報期間自動判定被整個關掉，理由是「操作員
  要求的警報不該被推翻」。結果是從按鈕開始的攔截永遠不會結束 — 子船
  會一直包夾一艘早就被趕出任務區的船，而整個任務的重點 (驅離、回去
  搜索) 只有在完全不碰按鈕的情況下才看得到。

  只有真的壓制過目標的接戰才會計為一次成功。在目標本來就已經在區外
  時按 INTERCEPT，八秒後會 stand down 而不是白算一次驅逐。

  要恢復舊行為 (INTERCEPT 變成只有 SURVEY 才能解除的鎖存)，把
  pTargetCoordinator 的 operator_release 設為 false。

  相關變數 (mothership → shoreside，可在 pMarineViewer scope 看到):
    SHIELD_STATE       = intercepting | searching
    SHIELD_DETECTIONS  = 累計偵測次數
    SHIELD_EVICTIONS   = 累計「驅逐成功」次數

  注意變數命名: 自動接戰發的是 TARGET_ALERT_AUTO / SURVEY_AUTO，
  不是 TARGET_ALERT_ALL / SURVEY_ALL。後者是從 shoreside 送進母船的，
  而 pShare 一旦對某個變數建立 output 路由，就拒絕把同名的 inbound
  注入本地 MOOSDB — 用同名發佈會把操作員自己的按鈕默默弄壞。

已知限制:
  - pRegionDivider 只做等寬長條切分，未依船速/電量做不等份分配。
  - RETURN 時 abe 與 ben 都回到 0,0 同一點。
  - perimeter 只走外框，覆蓋率格線中間不會填滿，這是預期行為。
  - 分帶較窄時 spiral 只夠繞一圈，結果會和 perimeter 相同。
  - sector 與 figure8 的路徑長度約為 lawnmower 的 2 倍，這是這兩種
    形狀本身的代價 (扇形刻意重複掃基準點附近、八字用淺轉彎換距離)，
    不是規劃缺陷。
  - COLREGs 的距離全部是照這個場地 (280 x 120 m) 與船速 (子船 2-3 m/s、
    入侵者 0.8-1.2 m/s) 縮放的，不是實船數值。換場地或換船速要重算:
    對遇合速 4 m/s 下，45m 的 pwt_outer_dist 約等於 11 秒的反應時間。
  - 驅離本質上就是要逼近他船。目前是「合規地逼近」— 讓路身分正確、
    從船尾而非船首通過、必要時減速、右舷留脫離航道 — 但它終究是
    刻意製造的接近局面，Rule 2 的「善良船藝」責任仍在操作員身上。
  - 未涵蓋的規則: Rule 5 (瞭望) 只有 contact manager 的外推; Rule 6
    (安全速度) 沒有明確的環境相依限速; Rule 19 (能見度受限) 沒有模擬。

詳細說明請參考: mission_documentation.md
